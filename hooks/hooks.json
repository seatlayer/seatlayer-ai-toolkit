{
  "description": "Reminds the assistant to consult SeatLayer's live contracts before editing SeatLayer integration code. Informational only; it never blocks or approves an edit.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/seatlayer-edit-reminder.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
